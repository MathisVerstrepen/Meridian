export enum SavingStatus {
    NOT_SAVED = 'NOT_SAVED',
    SAVING = 'SAVING',
    SAVED = 'SAVED',
    ERROR = 'ERROR',
    INIT = 'INIT',
}

export enum MessageRoleEnum {
    user = 'user',
    assistant = 'assistant',
    system = 'system',
}
